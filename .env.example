# LinkedIn OAuth Credentials
# Get these from: https://www.linkedin.com/developers/apps
LINKEDIN_CLIENT_ID=your_linkedin_client_id_here
LINKEDIN_CLIENT_SECRET=your_linkedin_client_secret_here

# Server Configuration
# Port the MCP server will listen on
PORT=8000

# Public URL of your server (used for OIDC callbacks)
# Development: http://localhost:8000
# Production: https://your-domain.com
SERVER_BASE_URL=http://localhost:8000

# OIDC Redirect URI (must match LinkedIn app settings)
# This should be: SERVER_BASE_URL + /auth/callback
LINKEDIN_REDIRECT_URI=http://localhost:8000/auth/callback

# OIDC Proxy Configuration (OPTIONAL - for production deployments)
# If not set, FastMCP uses in-memory storage (tokens lost on restart)

# JWT Signing Key - any sufficiently complex string for signing FastMCP tokens
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# If set, FastMCP will use encrypted disk storage automatically
# JWT_SIGNING_KEY=your_jwt_signing_key_here

# Token Encryption Key - only needed if you want to customize encryption
# By default, FastMCP derives encryption from JWT_SIGNING_KEY
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# TOKEN_ENCRYPTION_KEY=your_fernet_encryption_key_here

# Logging Configuration
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ============================================================================
# IMPORTANT SETUP INSTRUCTIONS
# ============================================================================
#
# 1. Create a LinkedIn OAuth App:
#    - Go to https://www.linkedin.com/developers/apps
#    - Create a new app or use an existing one
#    - Add the redirect URI: http://localhost:8000/auth/callback (for development)
#    - Request the following scopes: openid, profile, email, w_member_social
#    - Enable "Sign In with LinkedIn using OpenID Connect" product
#    - Copy the Client ID and Client Secret
#
# 2. Configure this .env file:
#    - Copy this file to .env (without the .example extension)
#    - Fill in your LINKEDIN_CLIENT_ID and LINKEDIN_CLIENT_SECRET
#    - Update SERVER_BASE_URL and LINKEDIN_REDIRECT_URI for production
#
# 3. For Production Deployments:
#    - Generate and set JWT_SIGNING_KEY and TOKEN_ENCRYPTION_KEY
#    - Configure TOKEN_STORAGE_DIR to point to a persistent volume
#    - Update SERVER_BASE_URL to your production domain
#    - Update LINKEDIN_REDIRECT_URI to match production URL
#    - Update your LinkedIn app's redirect URI to match production
#
# 4. OIDC Flow:
#    - Uses LinkedIn's OIDC configuration at:
#      https://www.linkedin.com/oauth/.well-known/openid-configuration
#    - MCP clients will register dynamically with your server
#    - Users will be redirected to LinkedIn to authorize
#    - Tokens are stored persistently (if keys are configured)
#    - Tokens are automatically refreshed when expired
#
# ============================================================================
